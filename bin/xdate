#!/usr/bin/bash

nowd() {
	if [ -z "$1" ]; then
		date +%y%m%d
	else
		date +%y"$1"%m"$1"%d
		shift 1
	fi
}

nowt() {
	if [ -z "$1" ]; then
		date +%H%M
	else
		date +%H"$1"%M
		shift 1
	fi
}

nowf() {
	if [ -z "$1" ]; then
		date +%y%m%d%H%M
	else
		date +%y"$1"%m"$1"%d"$1"%H"$1"%M
	fi
}
